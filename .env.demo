# .env.demo
# Environment configuration for DEMO server
# This file CAN be committed (no secrets)

APP_ENV=prod
APP_DEBUG=false

# AI Configuration for Demo
# Demo: Claude only (for now)
# Later: Switch to Ollama only
AI_ENABLED=true
AI_PROVIDER=claude

# Claude API (current setup)
AI_CLAUDE_ENABLED=true
# ANTHROPIC_API_KEY must be set via environment variable or GitHub Secret

# Ollama (for later migration)
# AI_CLAUDE_ENABLED=false
# OLLAMA_URL=http://ollama:11434
# OLLAMA_MODEL=llama3.1:8b
