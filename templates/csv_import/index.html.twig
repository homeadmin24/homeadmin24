{% extends 'base.html.twig' %}

{% block title %}CSV Import{% endblock %}

{% block body %}
    <div class="antialiased bg-gray-50 dark:bg-gray-900"
         data-controller="csv-import"
         data-csv-import-upload-url-value="{{ path('app_csv_upload') }}"
         data-csv-import-import-url-value="{{ path('app_csv_import_execute') }}"
         data-csv-import-zahlung-url-value="{{ path('app_zahlung_index') }}"
         data-csv-import-csrf-token-value="{{ csrf_token('csv_import') }}">
        {% include 'components/sidebar.html.twig' %}

        <main class="p-4 md:ml-64 h-auto pt-6">
            <div class="container mx-auto max-w-4xl">
                <!-- Header -->
                <div class="flex items-center mb-6">
                    <a href="{{ path('app_zahlung_index') }}" 
                       class="text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200 mr-4">
                        <i class="fas fa-arrow-left text-xl"></i>
                    </a>
                    <h1 class="text-2xl font-bold text-gray-900 dark:text-white flex items-center">
                        <i class="fas fa-file-csv mr-2"></i>
                        Kontoauszug importieren
                    </h1>
                </div>

                <!-- Upload Section -->
                <div id="upload-section"
                     data-csv-import-target="uploadSection"
                     class="bg-white dark:bg-gray-800 rounded-lg shadow-sm p-6 mb-6">
                    <h2 class="text-lg font-medium text-gray-900 dark:text-white mb-4">CSV-Datei hochladen</h2>

                    <form data-csv-import-target="uploadForm"
                          data-action="submit->csv-import#uploadCsv"
                          method="POST"
                          action="{{ path('app_csv_upload') }}"
                          enctype="multipart/form-data">
                        <input type="hidden" name="_token" value="{{ csrf_token('csv_upload') }}">
                        
                        <div class="mb-4">
                            <label for="csv_file" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
                                Kontoauszug (CSV-Datei)
                            </label>
                            <div class="mt-1 flex justify-center px-6 pt-5 pb-6 border-2 border-gray-300 border-dashed rounded-md">
                                <div class="space-y-1 text-center">
                                    <svg class="mx-auto h-12 w-12 text-gray-400" stroke="currentColor" fill="none" viewBox="0 0 48 48">
                                        <path d="M28 8H12a4 4 0 00-4 4v20m32-12v8m0 0v8a4 4 0 01-4 4H12a4 4 0 01-4-4v-4m32-4l-3.172-3.172a4 4 0 00-5.656 0L28 28M8 32l9.172-9.172a4 4 0 015.656 0L28 28m0 0l4 4m4-24h8m-4-4v8m-12 4h.02" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
                                    </svg>
                                    <div class="text-sm text-gray-600">
                                        <label for="csv_file" class="relative cursor-pointer bg-white rounded-md font-medium text-orange-600 hover:text-orange-500 focus-within:outline-none focus-within:ring-2 focus-within:ring-offset-2 focus-within:ring-orange-500">
                                            <span>CSV-Datei auswählen</span>
                                            <input id="csv_file"
                                                   name="csv_file"
                                                   type="file"
                                                   accept=".csv,text/csv,application/csv"
                                                   required
                                                   data-csv-import-target="fileInput"
                                                   data-action="change->csv-import#fileSelected"
                                                   class="sr-only">
                                        </label>
                                        <p class="mt-1">oder hier hineinziehen</p>
                                    </div>
                                    <p class="text-xs text-gray-500">KSK/Sparkasse CSV-Format mit SEPA-Daten</p>
                                </div>
                            </div>
                        </div>

                        <div class="flex justify-center" id="button-container">
                            <button type="submit"
                                    data-csv-import-target="uploadButton"
                                    class="inline-flex items-center px-6 py-3 border border-transparent text-base font-medium rounded-md shadow-sm text-white bg-orange-600 hover:bg-orange-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-orange-500">
                                <i class="fas fa-upload mr-2"></i>CSV analysieren und Vorschau anzeigen
                            </button>
                        </div>
                    </form>
                </div>

                <!-- Preview Section (initially hidden) -->
                <div data-csv-import-target="previewSection"
                     class="bg-white dark:bg-gray-800 rounded-lg shadow-sm p-6 hidden">
                    <h2 class="text-lg font-medium text-gray-900 dark:text-white mb-4">Vorschau</h2>
                    <div data-csv-import-target="previewContent"></div>
                </div>
            </div>
        </main>
    </div>
{% endblock %}